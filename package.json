{
  "name": "torrent-files-library",
  "version": "0.0.0-development",
  "description": "Enables to get a library of downloaded torrent files (movie or tv show) from a directory",
  "main": "index.js",
  "scripts": {
    "semantic-release": "semantic-release pre && npm publish && semantic-release post",
    "coverage": "node node_modules/.bin/babel-node node_modules/.bin/babel-istanbul cover node_modules/.bin/_mocha -- -R dot",
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "npm run compile && mocha"
  },
  "keywords": [
    "torrent",
    "library",
    "parser"
  ],
  "author": "jy95",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jy95/torrent-files-library.git"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-istanbul": "^0.12.2",
    "babel-preset-env": "^1.6.0",
    "babel-register": "^6.26.0",
    "coveralls": "^2.13.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.5.0",
    "semantic-release": "^7.0.2"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "filehound": "^1.16.2",
    "video-extensions": "^1.1.0"
  }
}
